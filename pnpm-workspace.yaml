allowBuilds:
  esbuild: set this to true or false
# pnpm 10/11：依赖的构建脚本需在此显式批准，否则 pnpm install 会报
# ERR_PNPM_IGNORED_BUILDS 并以非零码退出（Cloudflare Workers Builds 会判定失败）。
onlyBuiltDependencies:
  - esbuild
